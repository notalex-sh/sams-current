{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "routes": [
    {
      "src": "/(.*)",
      "transforms": [
        {
          "type": "response.headers",
          "op": "set",
          "target": { "key": "x-app-integrity" },
          "args": "enabled"
        }
      ]
    }
  ]
}
